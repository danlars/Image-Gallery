{{ content() }}


<div class="bs-callout bs-callout-warning">
    <h4>Unauthorized</h4>
    <p>You don't have access to this option.</p>
    <p>{{ link_to('login/index', 'Login page', 'class': 'btn btn-primary') }}</p>
</div>
